import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function SubscriptionScreen() {
  return (
    <PlaceholderScreen
      icon="star"
      title="Subscription"
      description="Billing/store integration is an open question (docs/OPEN_QUESTIONS.md) — never gates safety features."
    />
  );
}
