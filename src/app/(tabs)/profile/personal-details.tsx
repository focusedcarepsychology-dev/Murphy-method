import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function PersonalDetailsScreen() {
  return (
    <PlaceholderScreen
      icon="accountCircle"
      title="Personal details"
      description="Editable date of birth, height, weight, and units — wired to a real profile in Phase 2."
    />
  );
}
