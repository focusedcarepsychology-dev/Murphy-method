import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function ProfileEquipmentScreen() {
  return (
    <PlaceholderScreen
      icon="gear"
      title="Equipment"
      description="Mark equipment temporarily unavailable without deleting it — wired to real data in Phase 3."
    />
  );
}
