import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function ConsentScreen() {
  return (
    <PlaceholderScreen
      icon="shield"
      title="Consent"
      description="A list of your consent records, with the ability to withdraw where supported — wired in Phase 11."
    />
  );
}
