import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function BodyScanCompareScreen() {
  return (
    <PlaceholderScreen
      icon="camera"
      title="Compare scans"
      description="Side-by-side comparison via short-lived signed URLs is built in Phase 10."
    />
  );
}
