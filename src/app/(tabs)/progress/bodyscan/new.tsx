import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function NewBodyScanScreen() {
  return (
    <PlaceholderScreen
      icon="camera"
      title="New BodyScan"
      description="Camera capture and private storage upload are built in Phase 10 — no camera access exists yet."
    />
  );
}
