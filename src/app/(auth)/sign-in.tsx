import { PlaceholderScreen } from '@/components/dev/placeholder-screen';

export default function SignInScreen() {
  return (
    <PlaceholderScreen
      icon="key"
      title="Sign in"
      description="Real authentication connects to Supabase Auth in Phase 2. This is a visual shell only."
    />
  );
}
