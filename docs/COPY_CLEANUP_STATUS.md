# Visible Copy Cleanup

This branch removes user-facing em dashes, en dashes and generic AI-product phrasing from the app. It also adds regression coverage so those patterns cannot be reintroduced without failing CI.
