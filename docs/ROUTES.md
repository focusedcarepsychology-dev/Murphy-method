# Routes

Status: foundation only.

The project currently has two placeholder Expo Router routes generated by the
project template:

- `/` (`src/app/index.tsx`)
- `/explore` (`src/app/explore.tsx`)

No product screens, navigation flows, or route contracts have been designed
yet. This document will be expanded once `docs/MASTER_SPEC.md` has been
populated with an authoritative product specification.

Do not invent routes ahead of the spec.
