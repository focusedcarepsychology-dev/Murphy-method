# Database Schema

Status: not yet defined.

No data model exists yet. This document will describe the persisted schema
(tables/collections, fields, relationships, and migrations) once
`docs/MASTER_SPEC.md` has been populated with an authoritative product
specification.

Do not invent schema ahead of the spec.
